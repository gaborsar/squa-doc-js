export default class RangeItem {
    constructor(node, index, offset, length) {
        this.node = node;
        this.index = index;
        this.offset = offset;
        this.length = length;
    }
}
